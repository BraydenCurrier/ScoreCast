FONT_4x5_DATA = {
    # Numbers
    '0': [0xF, 0x9, 0x9, 0x9, 0xF],
    '1': [0x4, 0xC, 0x4, 0x4, 0xE],
    '2': [0xF, 0x1, 0xF, 0x8, 0xF],
    '3': [0xF, 0x1, 0xF, 0x1, 0xF],
    '4': [0x9, 0x9, 0xF, 0x1, 0x1],
    '5': [0xF, 0x8, 0xF, 0x1, 0xF],
    '6': [0xF, 0x8, 0xF, 0x9, 0xF],
    '7': [0xF, 0x1, 0x2, 0x4, 0x4],
    '8': [0xF, 0x9, 0xF, 0x9, 0xF],
    '9': [0xF, 0x9, 0xF, 0x1, 0xF],
    # Alphabet (4x5 matrices)
    'A': [0x6, 0x9, 0xF, 0x9, 0x9],
    'B': [0xE, 0x9, 0xE, 0x9, 0xE],
    'C': [0x7, 0x8, 0x8, 0x8, 0x7],
    'D': [0xE, 0x9, 0x9, 0x9, 0xE],
    'E': [0xF, 0x8, 0xE, 0x8, 0xF],
    'F': [0xF, 0x8, 0xE, 0x8, 0x8],
    'G': [0x7, 0x8, 0xB, 0x9, 0x7],
    'H': [0x9, 0x9, 0xF, 0x9, 0x9],
    'I': [0xE, 0x4, 0x4, 0x4, 0xE],
    'J': [0x3, 0x2, 0x2, 0xA, 0x4],
    'K': [0x9, 0xA, 0xC, 0xA, 0x9],
    'L': [0x8, 0x8, 0x8, 0x8, 0xF],
    'M': [0x9, 0xF, 0xF, 0x9, 0x9],
    'N': [0x9, 0xD, 0xB, 0x9, 0x9],
    'O': [0x6, 0x9, 0x9, 0x9, 0x6],
    'P': [0xE, 0x9, 0xE, 0x8, 0x8],
    'Y': [0x9, 0x9, 0x6, 0x2, 0x2],
    'Q': [0x6, 0x9, 0x9, 0xA, 0x5],
    'R': [0xE, 0x9, 0xE, 0xA, 0x9],
    'S': [0x7, 0x8, 0x6, 0x1, 0xE],
    'T': [0xF, 0x4, 0x4, 0x4, 0x4],
    'U': [0x9, 0x9, 0x9, 0x9, 0x6],
    'V': [0x9, 0x9, 0x9, 0x6, 0x2],
    'W': [0x9, 0x9, 0xF, 0xF, 0x9],
    'X': [0x9, 0x9, 0x6, 0x9, 0x9],
    'Z': [0xF, 0x2, 0x4, 0x8, 0xF],
    # Down Suffix Lowercase Letters (4x5)
    'd': [0x1, 0x7, 0x9, 0x9, 0x7],
    'h': [0x8, 0xE, 0x9, 0x9, 0x9],
    'n': [0x0, 0xC, 0x9, 0x9, 0x9],
    'r': [0x0, 0x5, 0x6, 0x4, 0x4],
    's': [0x0, 0x7, 0x6, 0x1, 0xE],
    't': [0x4, 0xE, 0x4, 0x4, 0x5],
    # Symbols / Formatting
    '-': [0x0, 0x0, 0xF, 0x0, 0x0],
    ':': [0x0, 0x4, 0x0, 0x4, 0x0],
    '&': [0x6, 0x4, 0xA, 0xA, 0x7]
}

MICRO_FONT = [
    [0x7, 0x5, 0x5, 0x5, 0x7],
    [0x2, 0x2, 0x2, 0x2, 0x2],
    [0x7, 0x1, 0x7, 0x4, 0x7],
    [0x7, 0x1, 0x7, 0x1, 0x7],
    [0x5, 0x5, 0x7, 0x1, 0x1],
    [0x7, 0x4, 0x7, 0x1, 0x7],
    [0x7, 0x4, 0x7, 0x5, 0x7],
    [0x7, 0x1, 0x1, 0x1, 0x1],
    [0x7, 0x5, 0x7, 0x5, 0x7],
    [0x7, 0x5, 0x7, 0x1, 0x7],
    [0x0, 0x0, 0x7, 0x0, 0x0],
    [0x0, 0x2, 0x0, 0x2, 0x0],
]

GFX_5X7 = {
    "A": [0x7C, 0x12, 0x11, 0x12, 0x7C],
    "B": [0x7F, 0x49, 0x49, 0x49, 0x36],
    "C": [0x3E, 0x41, 0x41, 0x41, 0x22],
    "D": [0x7F, 0x41, 0x41, 0x41, 0x3E],
    "E": [0x7F, 0x49, 0x49, 0x49, 0x41],
    "F": [0x7F, 0x09, 0x09, 0x09, 0x01],
    "G": [0x3E, 0x41, 0x49, 0x49, 0x7A],
    "H": [0x7F, 0x08, 0x08, 0x08, 0x7F],
    "I": [0x00, 0x41, 0x7F, 0x41, 0x00],
    "J": [0x20, 0x40, 0x41, 0x3F, 0x01],
    "K": [0x7F, 0x08, 0x14, 0x22, 0x41],
    "L": [0x7F, 0x40, 0x40, 0x40, 0x40],
    "M": [0x7F, 0x02, 0x0C, 0x02, 0x7F],
    "N": [0x7F, 0x04, 0x08, 0x10, 0x7F],
    "O": [0x3E, 0x41, 0x41, 0x41, 0x3E],
    "P": [0x7F, 0x09, 0x09, 0x09, 0x06],
    "Q": [0x3E, 0x41, 0x51, 0x21, 0x5E],
    "R": [0x7F, 0x09, 0x19, 0x29, 0x46],
    "S": [0x46, 0x49, 0x49, 0x49, 0x31],
    "T": [0x01, 0x01, 0x7F, 0x01, 0x01],
    "U": [0x3F, 0x40, 0x40, 0x40, 0x3F],
    "V": [0x1F, 0x20, 0x40, 0x20, 0x1F],
    "W": [0x3F, 0x40, 0x38, 0x40, 0x3F],
    "X": [0x63, 0x14, 0x08, 0x14, 0x63],
    "Y": [0x07, 0x08, 0x70, 0x08, 0x07],
    "Z": [0x61, 0x51, 0x49, 0x45, 0x43],

    "0": [0x3E, 0x51, 0x49, 0x45, 0x3E],
    "1": [0x00, 0x42, 0x7F, 0x40, 0x00],
    "2": [0x42, 0x61, 0x51, 0x49, 0x46],
    "3": [0x21, 0x41, 0x45, 0x4B, 0x31],
    "4": [0x18, 0x14, 0x12, 0x7F, 0x10],
    "5": [0x27, 0x45, 0x45, 0x45, 0x39],
    "6": [0x3C, 0x4A, 0x49, 0x49, 0x30],
    "7": [0x01, 0x71, 0x09, 0x05, 0x03],
    "8": [0x36, 0x49, 0x49, 0x49, 0x36],
    "9": [0x06, 0x49, 0x49, 0x29, 0x1E],

    "-": [0x08, 0x08, 0x08, 0x08, 0x08],
    " ": [0x00, 0x00, 0x00, 0x00, 0x00],
}

def draw_4x5_char(draw, x, y, char, color):
    """Draws a single 4x5 character safely from the font tracking table."""
    if char not in FONT_4x5_DATA:
        return

    for row in range(5):
        row_data = FONT_4x5_DATA[char][row]
        for col in range(4):
            if (row_data >> (3 - col)) & 1:
                draw.point((x + col, y + row), fill=color)

def print_4x5(draw, text, x, y, color):
    """Parses text and draws it on the canvas, handling colons and '1's with custom widths."""
    current_x = x
    text = str(text).upper()

    for char in text:
        if char == ' ':
            current_x += 3
        elif char == ':':
            draw_4x5_char(draw, current_x - 1, y, char, color)
            current_x += 2  # 1px width + 1px gap
        elif char == '1':
            # Shift back by 1 pixel to strip the blank column on the left
            draw_4x5_char(draw, current_x, y, char, color)
            current_x += 4  # 3px actual width + 1px trailing character gap
        elif char in FONT_4x5_DATA:
            draw_4x5_char(draw, current_x, y, char, color)
            current_x += 5  # 4px width + 1px gap
        else:
            current_x += 2

def get_4x5_width(text):
    """Calculates the exact pixel width of a 4x5 string, matching the tight '1' and colon logic."""
    total_width = 0
    text = str(text).upper()
    
    for char in text:
        if char == ' ':
            total_width += 3
        elif char == ':':
            total_width += 2
        elif char == '1':
            total_width += 4  # 3px actual width + 1px trailing character gap
        elif char in FONT_4x5_DATA:
            total_width += 5
        else:
            total_width += 2
            
    return total_width - 1 if total_width > 0 else 0

def print_4x5_centered(draw, text, center_x, y, color):
    """Prints a 4x5 font string anchored perfectly to a target horizontal midpoint."""
    # 1. Enforce string conversion to safely handle numbers, quarters, or timestamps
    text_str = str(text)
    
    # 2. Calculate the exact footprint width of the string
    string_width = get_4x5_width(text_str)
    
    # 3. Derive the exact tracking starting pixel coordinate
    # Integer division keeps it completely snapped to the matrix pixel grid
    start_x = center_x - (string_width // 2)
    
    # 4. Hand off execution to your layout engine loop
    print_4x5(draw, text_str, start_x, y, color)

def print_clock(draw, clock_string, center_x, y, color):
    
    clock_str = str(clock_string).strip().upper()
    
    # 1. Handle edge cases where there is no colon
    if ':' not in clock_str:
        print_4x5_centered(draw, clock_str, center_x, y, color)
        return

    # 2. Split the string into minutes and seconds
    time_parts = clock_str.split(':')
    minutes_str = time_parts[0]
    seconds_str = time_parts[1]
    
    if len(seconds_str) == 1:
        seconds_str = "0" + seconds_str

    # 3. Draw the colon locked at the center position
    # The dot sits at index 2. Subtracting 2 centers it exactly on center_x.
    colon_start_x = center_x - 2
    print_4x5(draw, ":", colon_start_x, y, color)
    
    # 4. Draw the minutes string to the left of the colon
    minutes_width = get_4x5_width(minutes_str)
    # Lock the right edge of the minutes string 1 pixel to the left of the colon box
    minutes_start_x = colon_start_x - minutes_width - 1
    print_4x5(draw, minutes_str, minutes_start_x, y, color)
    
    # 5. Draw the seconds string to the right of the colon
    # NEW: Move seconds left by 1 pixel (colon_start_x + 3 instead of + 4)
    # This pulls the seconds tight against the centered dot for a perfect 1px gap.
    seconds_start_x = colon_start_x + 2
    print_4x5(draw, seconds_str, seconds_start_x, y, color)

def draw_3x5_glyph(draw, x, y, index, color):
    if index < 0 or index > 11:
        return

    for row in range(5):
        row_data = MICRO_FONT[index][row]
        for col in range(3):
            if (row_data >> (2 - col)) & 1:
                draw.point((x + col, y + row), fill=color)

def print_3x5(draw, text, x, y, color):
    current_x = x

    for c in str(text):
        if c.isdigit():
            draw_3x5_glyph(draw, current_x, y, int(c), color)
            current_x += 4
        elif c == "-":
            draw_3x5_glyph(draw, current_x, y, 10, color)
            current_x += 4
        elif c == ":":
            draw_3x5_glyph(draw, current_x, y, 11, color)
            current_x += 4
        elif c == " ":
            current_x += 3

def get_3x5_width(text):
    total = 0

    for c in str(text):
        if c.isdigit() or c in "-:":
            total += 4
        elif c == " ":
            total += 3

    return total - 1 if total > 0 else 0

def print_gfx_5x7(draw, text, x, y, color):
    cursor_x = x

    for char in str(text).upper():
        bitmap = GFX_5X7.get(char, GFX_5X7[" "])

        for col in range(5):
            column_data = bitmap[col]

            for row in range(7):
                if (column_data >> row) & 1:
                    draw.point((cursor_x + col, y + row), fill=color)

        cursor_x += 6

def gfx_5x7_width(text):
    return len(str(text)) * 6 - 1

def draw_text_right(draw, text, right_x, y, color):
    text = str(text)

    width = gfx_5x7_width(text)

    print_gfx_5x7(
        draw,
        text,
        right_x - width,
        y,
        color
    )